function add(a: number, b: number) {
    return a + b
}

// Arrow Function
const greet = (name: string) => 'Hi, ' + name + ' welcome';

const substract = (a: number, b: number): number => {
    // use if statement
    if(b > a) {
        return b - a
    }else if() {

    }else {
        
    }
    return a - b;
}

// console.log(greeting())
console.log(greet("Olawale"))

const addition = add(4, 89)
console.log(substract(4, 10))

// 

